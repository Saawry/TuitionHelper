<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gadware.tution">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TuitionHelper">
        <activity android:name=".Activities.TuitionDetails"></activity>
        <activity android:name=".Activities.AddNewSession" />
        <activity android:name=".Activities.WeeklySchedule" />
        <activity android:name=".Activities.UserProfile" />
        <activity android:name=".Activities.ProfileSetupActivity" />
        <activity android:name=".Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.AddNewTuition" />
        <activity android:name=".Activities.MainActivity" />
    </application>

</manifest>